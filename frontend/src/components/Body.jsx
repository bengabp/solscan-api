import React from 'react';
import { AppBar, Toolbar, Typography, Box, LinearProgress, Stack } from '@mui/material';

const Body = (props) => {

    return (
        <Stack>
            <Typography>something test</Typography>
        </Stack>
    );
    
}

export default Body;